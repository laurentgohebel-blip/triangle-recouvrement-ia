{
  "buildCommand": "echo 'No build needed'",
  "outputDirectory": ".",
  "framework": "vanilla",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}